# DVC Pipeline — Datathon Fase 05
# ============================================
# Reproduzir: dvc repro

stages:
  prepare_data:
    cmd: python -m src.features.feature_engineering
    deps:
      - data/raw/
      - src/features/feature_engineering.py
    outs:
      - data/processed/features.parquet

  train_baseline:
    cmd: python -m src.models.train
    deps:
      - data/processed/features.parquet
      - src/models/train.py
      - src/models/baseline.py
    metrics:
      - metrics/train_metrics.json:
          cache: false

  evaluate:
    cmd: python -m evaluation.ragas_eval
    deps:
      - data/golden_set/golden_set.json
      - evaluation/ragas_eval.py
      - src/agent/rag_pipeline.py
    metrics:
      - metrics/ragas_metrics.json:
          cache: false
